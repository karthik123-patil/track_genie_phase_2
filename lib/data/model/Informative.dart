class InfoMedia {
  int id;
  String url;
  String title;
  String length;
  String type;
  String fileSize;

  InfoMedia(
      this.id, this.url, this.title, this.length, this.type, this.fileSize);
}
